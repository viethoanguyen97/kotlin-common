package com.hovispace.kotlincommons.kotlinbasic;

class StringUtils {

    // NOTE: When defining a method in java that return type is a void, in Kotlin returned value will be of a Unit type.
    public static String toUpperCase(String name) {
        return name.toUpperCase();
    }
}
